export enum NodeType {
  document = 'document',
  header = 'header',
  paragraph = 'paragraph',
  template = 'template',
  text = 'text',
  list = 'list',
  task = 'taskgraph',
  image = 'image',
  video = 'video',
  canvas = 'canvas',
  raster = 'raster',
}